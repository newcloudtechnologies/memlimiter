version: "3.3"
services:
  allocator:
    build: ${PWD}/allocator
    network_mode: host
    deploy:
      resources:
        limits:
          memory: 1G
    volumes:
      - ${PWD}/allocator:/etc/allocator
